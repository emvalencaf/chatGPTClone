// component
import Button from "../Button";

// styles
import * as Styled from "./styles";

const SideMenu = () => {
	return (
		<Styled.Wrapper>
			<Button>Novo Chat</Button>
		</Styled.Wrapper>
	);
};

export default SideMenu;
